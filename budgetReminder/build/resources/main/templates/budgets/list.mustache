{{>layouts/header}}

<style>
    .flex-container {
        display: flex;
        gap: 20px;
    }
    .left-table {
        flex: 1;
    }
    .right-form {
        flex: 1;
    }

    .button-group {
        display: flex;
        justify-content: center;
        gap: 10px;
        margin-top: 30px;
    }
</style>

<table class="table">
    <thead>
    <tr>
        <th scope="col">#</th>
        <th scope="col">MEMO</th>
        <th scope="col">CASH</th>
        <th scope="col">EDIT</th>
        <th scope="col">DELETE</th>
    </tr>
    </thead>
    <tbody>
    {{#budgetList}}
        <tr>
            <th>{{id}}</th>
            <td>{{memo}}</td>
            <td>{{cash}}</td>
            <td><a href="/budgets/{{id}}/edit" class="btn btn-primary">수정</a>
            <td><a href="/budgets/{{id}}/delete" class="btn btn-primary">삭제</a>
        </tr>
    {{/budgetList}}
    </tbody>
</table>
<div class="button-group" >
    <a href="/main" class="btn btn-primary">메인</a>
    <a href="/budgets" class="btn btn-primary">뒤로</a>
</div>

{{>layouts/footer}}